//3. Multiply two integers and display the product. (Hint: use * operator)
#include<stdio.h>
int main(void)
{
    int a, b,c;
    printf("\nEnter the the first number:");
    scanf("%d",&a);
    printf("\nEnter the the second number:");
    scanf("%d",&b);
    c=a*b;
    printf("\nThe product of first and the second numbers are: %d \n",c);
    return 0;
}
